server:
  port: 9091

log:
  level: DEBUG

spring:
  application:
    name: EurekaServer

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/
    register-with-eureka: false
    fetch-registry: false

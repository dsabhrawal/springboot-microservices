server:
  port: 9093

log:
  level: DEBUG

spring:
  application:
    name: ProductService
  cloud:
    config:
      url: http://localhost:9090

eureka:
  client:
    service:
      url:
        defaultZone: http://localhost:9091/eureka
security:
  basic:
    enable: false

management:
  security:
    enabled: false

